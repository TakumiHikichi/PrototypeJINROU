<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jinrou2" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".FirstActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RuleActivity"/>
        <activity android:name=".SettingActivity"/>
        <activity android:name=".GameActivity_setting"/>
        <activity android:name=".GameActivity_main"/>
        <activity android:name=".GameActivity_confirm"/>
        <activity android:name=".GameActivity_end"/>
    </application>

</manifest>
